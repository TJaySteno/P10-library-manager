extends ../layout

block content
  p.
    #[strong Book Title:] #[span= title]
  p.
    #[strong Loaned By:] #[span= patron]
  p.
    #[strong Loaned On:] #[span= loaned_on]
  p.
    #[strong Return By:] #[span= return_by]
  form
    p
      label(for="returned_on") Returned on:
      input(id="returned_on" type="text" value=`${now}` name="returned_on")
    p
      input(type="submit" value="Return Book")
