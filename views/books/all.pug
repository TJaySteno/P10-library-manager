extends table

block books-table-content
  each book in books
    tr
      td
        a(href=`/books/details/${book.id}`)= book.title
      td= book.author
      td= book.genre
      td= book.first_published
